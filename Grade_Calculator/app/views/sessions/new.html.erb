<!DOCTYPE html>
<!-- HTML file for Homepage of ITIS 3135 Project -->

<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Class Grade Calculator</title>
    <link rel="stylesheet" type="text/css" href="../stylesheets/main.css">
    <link rel="icon" type="image/x-icon" href="https://my.uncc.edu/sites/all/themes/custom/my_unc_charlotte/favicon.ico"/>
</head>

<body>
<header>

</header>

<main>
    <div class="content">
        <h1 class="titleText">Class Grade Calculator</h1>
        <p id="intro">
            Please login to use the calculator.
        </p>
        <div class="formDiv">
                <%= form_with :class=> "studentInfo", url: sessions_path do |form| %>
                    <% flash.each do |name, msg| %>
                        <div id="error_explanation">
                          <h2>Errors prohibited from logging in: </h2>
                          <ul>
                            <li>Either incorrect email/password</li>
                            <li>Or user doesn't exists</li>
                          </ul>
                        </div>
                    <% end %>
                    <p><%= form.label("Email:", :class=> "fieldTitle") %> </p>
                    <%= form.text_field :email, :class=> "inputField" %>
                    <p class="fieldTitle"><%= form.label("Password:") %> </p>
                    <%= form.password_field :password, :class=>"inputField" %>
                    <br><br>
                    <%= form.submit("Login", :class=>"submitButton") %>
                    &nbsp; &nbsp;
                    <%= link_to 'Back', users_new_path, :class=> "otherButton" %>
                <% end %>
        </div>
        <div>
            <%= image_tag("studying.jpg", :id=>"studyingPicture", :alt=>"Studying Picture", :width=>"400px") %>
        </div>
    </div>
</main>

<footer>
</footer>

</body>
</html>